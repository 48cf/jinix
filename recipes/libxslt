name=libxslt
from_source=libxslt
revision=1
builddeps="base python"
deps="core-libs libxml"

build() {
    autotools_configure_lib \
        --docdir=${prefix}/share/doc/libxslt-${version}

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
