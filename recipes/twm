name=twm
revision=1
from_source=twm
hostdeps="gcc pkg-config"
deps="core-libs xorg-server xsetroot"

build() {
    STRIP=${OS_TRIPLET}-strip autotools_configure

    make -j${parallelism}
}

package() {
    make DESTDIR=${dest_dir} install-strip
}
