name=lzip
from_source=lzip
revision=1
builddeps="base"
deps="core-libs"

build() {
    autotools_configure \
        CXXFLAGS="$CXXFLAGS" \
        LDFLAGS="$LDFLAGS"

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
