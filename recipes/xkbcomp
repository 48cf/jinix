name=xkbcomp
from_source=xkbcomp
revision=1
builddeps="base"
deps="core-libs xorg-util-macros libx11 libxkbfile"

build() {
    autotools_configure

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
