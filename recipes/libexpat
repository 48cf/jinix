name=libexpat
from_source=libexpat
revision=1
hostdeps="gcc pkg-config"
deps="core-libs"

build() {
    autotools_configure_lib \
        --without-xmlwf

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
