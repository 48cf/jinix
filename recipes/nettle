name=nettle
from_source=nettle
revision=1
hostdeps="gcc pkg-config"
deps="core-libs gmp"
imagedeps="gcc"

build() {
    autotools_configure_lib \
        --disable-documentation

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
