name=nettle
from_source=nettle
revision=1
builddeps="base"
deps="core-libs gmp"

build() {
    autotools_configure_lib \
        --disable-documentation

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
