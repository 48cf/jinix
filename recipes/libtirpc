name=libtirpc
from_source=libtirpc
revision=1
builddeps="base"
deps="core-libs"

build() {
    autotools_configure_lib \
        --disable-gssapi

    make -j${parallelism}
}

package() {
    DESTDIR=${dest_dir} make install

    post_package_strip
}
