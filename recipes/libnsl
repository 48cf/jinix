name=libnsl
from_source=libnsl
revision=1
builddeps="base"
deps="core-libs libtirpc"

build() {
    autotools_configure_lib

    make -j${parallelism}
}

package() {
    DESTDIR=${dest_dir} make install

    post_package_strip
}
