name=libpciaccess
from_source=libpciaccess
revision=1
builddeps="base"
deps="core-libs zlib"

build() {
    autotools_configure_lib \
        --with-zlib

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
