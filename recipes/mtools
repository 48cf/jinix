name=mtools
from_source=mtools
revision=1
builddeps="base"
deps="core-libs"

build() {
    export CFLAGS="${CFLAGS/-Wp,-D_FORTIFY_SOURCE=2/}"

        LIBS="-liconv" \
    autotools_configure

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
