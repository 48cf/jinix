name=xorg-proto
from_source=xorg-proto
revision=1
hostdeps="gcc pkg-config"
deps="core-libs"

build() {
    autotools_configure
    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
