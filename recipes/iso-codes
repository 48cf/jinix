name=iso-codes
from_source=iso-codes
revision=1
builddeps="base gettext python"
deps="core-libs"

build() {
    cp -rp "${source_dir}"/. ./

        configure_script_path=./configure \
    autotools_configure

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install
}
