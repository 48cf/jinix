# XXX this is provided by python 3.11, but we need to revert commit containing
# this change when we upgrade to 3.12 because of [PEP 632](https://peps.python.org/pep-0632/)

name=python-setuptools
from_source=python-setuptools
builddeps="base"
deps="python"

build() {
    true
}

package() {
    true
}
