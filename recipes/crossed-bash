cross_compile=yes

name=crossed-bash
from_source=bash
revision=1
imagedeps="gcc"
hostdeps="gcc pkg-config"
deps="crossed-core-libs crossed-ncurses crossed-readline"

build() {
    autotools_configure \
        --without-bash-malloc  \
        --with-installed-readline="${sysroot}/usr/lib" \
        --disable-nls

    make -j${parallelism}
}

package() {
    make install DESTDIR="${dest_dir}"
    ln -s bash "${dest_dir}${prefix}"/bin/sh

    post_package_strip
}
