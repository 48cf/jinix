cross_compile=yes

name=xz-utils
from_source=xz-utils
revision=1
hostdeps="gcc pkg-config"
deps="core-libs zlib"

build() {
    autotools_configure \
        --disable-static

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
