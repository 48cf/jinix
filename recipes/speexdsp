name=speexdsp
from_source=speexdsp
revision=1
builddeps="base"
deps="core-libs libogg"

build() {
    autotools_configure_lib \
        --docdir="${prefix}/share/doc/speexdsp-${version}"

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
