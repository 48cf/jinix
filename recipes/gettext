name=gettext
from_source=gettext
revision=1
builddeps="base"
deps="core-libs"

build() {
    autotools_configure \
        --disable-static

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
