name=libxau
from_source=libxau
revision=1
builddeps="base"
deps="core-libs xorg-proto"

build() {
    autotools_configure_lib

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
