name=libxpm
from_source=libxpm
revision=1
hostdeps="gcc pkg-config"
deps="core-libs xorg-util-macros xorg-proto libx11 libxext libxt"

build() {
    LIBS="-lintl" autotools_configure_lib
    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
