cross_compile=yes

name=crossed-mpfr
from_source=mpfr
revision=1
imagedeps="gcc"
hostdeps="gcc pkg-config"
deps="crossed-core-libs crossed-gmp"

build() {
    cp -rp "${source_dir}"/. ./

        configure_script_path=./configure \
    autotools_configure \
        --enable-static=no \
        --enable-shared=yes \
        --with-pic

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
