name=psmisc
from_source=psmisc
revision=1
hostdeps="gcc pkg-config"
deps="core-libs ncurses"

build() {
    autotools_configure \
        --enable-nls \
        --disable-static

    make -j${parallelism}
}

package() {
    DESTDIR=${dest_dir} make install
}
