name=psmisc
from_source=psmisc
revision=1
builddeps="base"
deps="core-libs ncurses"

build() {
    autotools_configure \
        --enable-nls \
        --disable-static

    make -j${parallelism}
}

package() {
    DESTDIR=${dest_dir} make install

    post_package_strip
}
