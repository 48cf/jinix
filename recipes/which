cross_compile=yes

name=which
from_source=which
revision=1
hostdeps="gcc"
deps="core-libs"

build() {
        AR=${OS_TRIPLET}-ar \
    autotools_configure

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
