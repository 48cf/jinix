name=libglvnd
from_source=libglvnd
revision=1
builddeps="base"
deps="core-libs libx11 libxext"

build() {
    autotools_configure_lib \
        --enable-x11 \
        --enable-glx

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
