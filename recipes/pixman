name=pixman
from_source=pixman
revision=1
builddeps="base"
deps="core-libs libpng"

build() {
    autotools_configure_lib

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
