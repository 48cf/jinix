name=fribidi
from_source=fribidi
revision=1
imagedeps="gcc"
hostdeps="gcc pkg-config"
deps="core-libs"

build() {
    autotools_configure_lib
    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
