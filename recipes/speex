name=speex
from_source=speex
revision=1
builddeps="base"
deps="core-libs libogg speexdsp"

build() {
    autotools_configure_lib \
        --docdir="${prefix}/share/doc/speex-${version}"

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
