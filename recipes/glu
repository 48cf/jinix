name=glu
from_source=glu
revision=1
hostdeps="gcc pkg-config"
deps="core-libs mesa"

build() {
    autotools_configure_lib
    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install-strip
}
