name=libxcomposite
from_source=libxcomposite
revision=1
builddeps="base"
deps="core-libs libxfixes xorg-util-macros xorg-proto"

build() {
    autotools_configure_lib

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
