diff -urN --no-dereference libical-clean/cmake/modules/FindGObjectIntrospection.cmake libical-workdir/cmake/modules/FindGObjectIntrospection.cmake
--- libical-clean/cmake/modules/FindGObjectIntrospection.cmake
+++ libical-workdir/cmake/modules/FindGObjectIntrospection.cmake
@@ -42,11 +42,31 @@
   pkg_check_modules(_pc_gir gobject-introspection-1.0${_gir_version_cmp})
   if(_pc_gir_FOUND)
     set(GObjectIntrospection_FOUND TRUE)
-    _gir_get_pkgconfig_var(GObjectIntrospection_SCANNER "g_ir_scanner")
-    _gir_get_pkgconfig_var(GObjectIntrospection_COMPILER "g_ir_compiler")
-    _gir_get_pkgconfig_var(GObjectIntrospection_GENERATE "g_ir_generate")
-    _gir_get_pkgconfig_var(GObjectIntrospection_GIRDIR "girdir")
-    _gir_get_pkgconfig_var(GObjectIntrospection_TYPELIBDIR "typelibdir")
+    if(DEFINED ENV{INTROSPECTION_SCANNER_ENV})
+      set(GObjectIntrospection_SCANNER "$ENV{INTROSPECTION_SCANNER_ENV}")
+    else()
+      _gir_get_pkgconfig_var(GObjectIntrospection_SCANNER "g_ir_scanner")
+    endif()
+    if(DEFINED ENV{INTROSPECTION_COMPILER_ENV})
+      set(GObjectIntrospection_COMPILER "$ENV{INTROSPECTION_COMPILER_ENV}")
+    else()
+      _gir_get_pkgconfig_var(GObjectIntrospection_COMPILER "g_ir_compiler")
+    endif()
+    if(DEFINED ENV{INTROSPECTION_GENERATE_ENV})
+      set(GObjectIntrospection_GENERATE "$ENV{INTROSPECTION_GENERATE_ENV}")
+    else()
+      _gir_get_pkgconfig_var(GObjectIntrospection_GENERATE "g_ir_generate")
+    endif()
+    if(DEFINED ENV{INTROSPECTION_GIRDIR_ENV})
+      set(GObjectIntrospection_GIRDIR "$ENV{INTROSPECTION_GIRDIR_ENV}")
+    else()
+      _gir_get_pkgconfig_var(GObjectIntrospection_GIRDIR "girdir")
+    endif()
+    if(DEFINED ENV{INTROSPECTION_TYPELIBDIR_ENV})
+      set(GObjectIntrospection_TYPELIBDIR "$ENV{INTROSPECTION_TYPELIB_ENV}")
+    else()
+      _gir_get_pkgconfig_var(GObjectIntrospection_TYPELIBDIR "typelibdir")
+    endif()
     set(GObjectIntrospection_CFLAGS "${_pc_gir_CFLAGS}")
     set(GObjectIntrospection_LIBS "${_pc_gir_LIBS}")
   endif()
